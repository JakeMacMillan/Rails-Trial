$("ul.errors").html("")
<% if @game.errors.any? %>
  <% @game.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $(".game-index").html("<%= j render('index') %>")
  $("#game-modal").modal("hide")
<% end %>